# Go
/observe
*.exe
*.test

# Compiled bench binary (rebuilt with `go build` in ./bench)
bench/observe-bench
# Bench output — volatile per run; shouldn't track changes
bench_results.json

# Nucleus data
nucleus_data/

# Node
node_modules/
ui/node_modules/

# Build artifacts
# (cmd/observe/ui/dist is committed — it's an embed target. Regenerated by
# scripts/ui-sync.sh during release. UI sources still live in ui/src/.)

# (internal/geo/geo_ranges.bin is committed — it's an embed target.
# Regenerate when needed: cd internal/geo && go run gen_binary.go.)

# Environment
.env

# Reference codebases (large, not our code)
ref/umami/
ref/posthog/
ref/signoz/
ref/sentry/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Claude Code worktrees + session state — should never be committed
.claude/

# Internal planning and launch notes — local only, not shipped to the repo
MASTER_PLAN.md
marketing/launch/

# Private notes — local only, never committed
/_internal/
/INTERNAL.md

# Private infra deploy overlay (Tailscale server IP)
teploy.home.yml

# Private deploy overlays with real secrets inline (session salt, JWT
# secret, admin password) — this repo mirrors to public GitHub, so any
# teploy.*.yml beyond the base teploy.yml must never be trackable.
teploy.*.yml
!teploy.yml

# Private dev guide (internal context — never committed, cf. teploy-cli)
CLAUDE.md

# Python bytecode. Three .pyc files were committed under sdk/python/, so every
# checkout shipped compiled artefacts with the SDK and every working tree read
# as dirty the moment anything imported it.
__pycache__/
*.py[cod]

# Local compose overrides are, by definition, local.
docker-compose.override.yml
